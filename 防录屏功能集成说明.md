# 防录屏功能集成说明

## 概述

本项目已成功集成了全局防录屏、截屏功能，适用于uni-app框架开发的Vue3银行应用。该功能可以有效防止用户录屏、截屏，保护应用中的敏感信息。

## 功能特性

### 🛡️ 多级防护
- **低级防护**: 基础截屏检测和提示
- **中级防护**: 增强检测 + 禁用右键菜单和文本选择
- **高级防护**: 最严格保护 + 开发者工具检测 + 防护水印

### 🎯 全平台支持
- **H5网页版**: 完整功能支持
- **微信小程序**: 支持截屏检测
- **App原生**: 支持Android FLAG_SECURE
- **其他小程序**: 基础检测功能

### 📊 智能监控
- 实时截屏/录屏检测
- 开发者工具检测
- 快捷键拦截
- 安全事件记录和上报

## 文件结构

```
src/
├── utils/
│   └── screen-protector.js          # 核心防录屏工具类
├── plugins/
│   └── screen-protector-plugin.js   # Vue插件配置
├── pages/user/
│   └── screen-protection.vue        # 防录屏设置页面
├── components/
│   └── ScreenProtectionDemo.vue     # 功能演示组件
├── main.js                          # 插件注册
├── App.vue                          # 全局初始化
└── pages.json                       # 路由配置
```

## 安装与配置

### 1. 自动启用 (推荐)
应用启动时会自动启用防录屏功能，使用高级防护级别。

### 2. 手动控制
```javascript
// 启用防护
this.$enableScreenProtection({
  level: 'high',           // 防护级别: low/medium/high
  showAlert: true,         // 显示警告
  showWatermark: true,     // 显示水印
  watermarkText: '银行APP - 隐私保护中'
})

// 禁用防护
this.$disableScreenProtection()

// 获取状态
const status = this.$getScreenProtectionStatus()

// 获取安全事件
const events = this.$getSecurityEvents()
```

### 3. 页面级别配置
```javascript
// 在页面中使用装饰器
import { withScreenProtection } from '@/plugins/screen-protector-plugin.js'

export default withScreenProtection({
  level: 'high',
  disableOnUnload: false
})({
  // 页面配置
})
```

## 防护机制说明

### H5网页版防护
- ✅ 禁用右键菜单
- ✅ 拦截快捷键 (F12, Ctrl+Shift+I, PrintScreen等)
- ✅ 禁用文本选择和拖拽
- ✅ 开发者工具检测
- ✅ 防护水印显示
- ✅ 页面可见性监控

### 小程序防护
- ✅ 截屏事件监听 (微信小程序)
- ✅ 基础安全提示
- ⚠️ 功能受平台限制

### App原生防护
- ✅ Android FLAG_SECURE标志
- ✅ 应用后台保护
- ✅ 系统级截屏阻止
- ⚠️ iOS功能相对有限

## 用户设置

用户可以通过 `个人中心 > 隐私保护` 进行以下设置：

1. **开启/关闭防护**: 总开关控制
2. **防护级别**: 选择低级/中级/高级防护
3. **警告提示**: 检测到可疑行为时是否显示警告
4. **水印保护**: 是否在页面显示防护水印
5. **安全记录**: 查看、导出、清除安全事件记录

## 安全事件记录

系统会自动记录以下安全事件：
- 📷 截屏检测
- 📹 录屏检测  
- 🔧 开发者工具检测
- 🚫 被阻止的操作

每个事件包含：
- 事件类型和时间
- 用户信息 (如已登录)
- 页面路径
- 设备信息

## 技术实现

### 核心原理
1. **事件监听**: 监听系统和浏览器事件
2. **行为拦截**: 阻止危险操作
3. **实时检测**: 持续监控可疑行为
4. **智能响应**: 根据威胁级别采取不同措施

### 兼容性
- ✅ Vue 3 + uni-app
- ✅ 各主流浏览器
- ✅ 微信、支付宝等小程序
- ✅ Android/iOS原生App

## 最佳实践

### 1. 敏感页面强化保护
```javascript
// 在银行卡、交易等敏感页面
onShow() {
  this.$enableScreenProtection({
    level: 'high',
    showWatermark: true
  })
}
```

### 2. 智能防护策略
```javascript
// 根据页面内容调整防护级别
const sensitivePages = ['/pages/credit-card/', '/pages/transfer/']
const currentPage = getCurrentPages().pop()?.route

if (sensitivePages.some(page => currentPage.includes(page))) {
  this.$enableScreenProtection({ level: 'high' })
} else {
  this.$enableScreenProtection({ level: 'medium' })
}
```

### 3. 事件处理
```javascript
// 监听安全事件
this.$screenProtector.on('onScreenshotDetected', (data) => {
  // 发送风险预警到后端
  this.reportSecurityRisk('screenshot', data)
  
  // 强制用户重新登录
  this.forceRelogin()
})
```

## 注意事项

### 1. 用户体验
- 避免过度弹窗影响用户体验
- 提供明确的功能说明
- 允许用户自定义防护级别

### 2. 技术限制
- 不同平台功能支持程度不同
- 无法100%阻止所有录屏行为
- 需要结合服务端安全策略

### 3. 合规考虑
- 遵守相关法律法规
- 尊重用户隐私权
- 提供透明的功能说明

## 故障排除

### 常见问题

**Q: 防护功能不生效？**
A: 检查浏览器兼容性，确保JavaScript执行正常

**Q: 影响正常操作？**
A: 调整防护级别或在特定页面临时禁用

**Q: 小程序功能受限？**
A: 小程序环境功能有限，建议使用基础防护

### 调试方法
```javascript
// 开启调试模式
console.log('防护状态:', this.$getScreenProtectionStatus())
console.log('安全事件:', this.$getSecurityEvents())

// 模拟安全事件测试
this.$screenProtector.handleScreenshotDetected()
```

## 更新日志

### v1.0.0 (2024-09-11)
- ✅ 完成基础防录屏功能
- ✅ 支持多级防护策略
- ✅ 实现用户设置界面
- ✅ 添加安全事件记录
- ✅ 完成全局插件集成

## 联系支持

如有技术问题或功能建议，请联系开发团队。

---

> 🛡️ **安全提醒**: 防录屏功能只是安全策略的一部分，建议结合其他安全措施如数据加密、访问控制等，构建多层次的安全防护体系。
