# 页面跳转错误修复说明

## 问题描述

从图片中可以看到，水费页面在尝试跳转到城市选择页面时出现了 `navigateTo:fail page '/pages/city-select/city-select' is not found` 的错误。

## 问题分析

通过检查发现，主要问题是：

1. **页面未在pages.json中注册**：城市选择页面和水费缴费页面存在但未在pages.json中注册
2. **缺失的页面跳转**：一些页面引用了不存在的页面
3. **页面配置不完整**：部分页面缺少必要的配置

## 修复内容

### 1. 添加缺失的页面配置

在 `src/pages.json` 中添加了以下页面配置：

```json
{
  "path": "pages/water-payment/water-payment",
  "style": {
    "navigationBarTitleText": "水费缴费",
    "enablePullDownRefresh": false
  }
},
{
  "path": "pages/city-select/city-select",
  "style": {
    "navigationBarTitleText": "选择城市",
    "enablePullDownRefresh": false
  }
},
{
  "path": "pages/recharge-payment/recharge-payment",
  "style": {
    "navigationBarTitleText": "支付",
    "enablePullDownRefresh": false
  }
}
```

### 2. 修复页面跳转问题

#### 2.1 转账页面 (`src/pages/transfer/transfer.vue`)
- **问题**：跳转到不存在的联系人页面 `/pages/transfer/contacts`
- **修复**：将跳转改为提示信息

```javascript
// 修复前
showAllContacts() {
  uni.navigateTo({
    url: '/pages/transfer/contacts'
  })
}

// 修复后
showAllContacts() {
  uni.showToast({
    title: "联系人功能开发中",
    icon: "none"
  })
}
```

#### 2.2 登录页面 (`src/pages/denglu/login.vue`)
- **问题**：跳转到不存在的忘记密码页面 `/pages/forget/forget`
- **修复**：将跳转改为提示信息

```javascript
// 修复前
<navigator url="/pages/forget/forget" class="help-link">
  <text class="link-text">忘记密码</text>
</navigator>

// 修复后
<view class="help-link" @tap="showForgetPasswordTip">
  <text class="link-text">忘记密码</text>
</view>

// 添加方法
showForgetPasswordTip() {
  uni.showModal({
    title: "忘记密码",
    content: "请联系客服或前往银行网点重置密码",
    showCancel: false,
    confirmText: "确定"
  })
}
```

## 修复后的页面配置

### 新增页面配置
1. **水费缴费页面** (`pages/water-payment/water-payment`)
   - 标题：水费缴费
   - 功能：处理水费缴费流程

2. **城市选择页面** (`pages/city-select/city-select`)
   - 标题：选择城市
   - 功能：选择缴费城市

3. **支付页面** (`pages/recharge-payment/recharge-payment`)
   - 标题：支付
   - 功能：处理各种支付流程

## 测试验证

### 1. 水费缴费流程测试
```
生活缴费 → 水费 → 选择城市 → 选择供水公司 → 水费缴费 → 支付
```

### 2. 页面跳转测试
- ✅ 水费页面 → 城市选择页面
- ✅ 水费页面 → 水费缴费页面
- ✅ 水费缴费页面 → 支付页面
- ✅ 转账页面 → 联系人功能提示
- ✅ 登录页面 → 忘记密码提示

## 注意事项

### 1. 页面注册
- 所有页面都必须在 `pages.json` 中注册才能正常跳转
- 页面路径必须与实际文件路径一致

### 2. 跳转方式
- 使用 `uni.navigateTo()` 进行页面跳转
- 使用 `uni.switchTab()` 跳转到TabBar页面
- 使用 `uni.reLaunch()` 重新启动到指定页面

### 3. 错误处理
- 对于不存在的页面，提供友好的提示信息
- 避免硬编码不存在的页面路径

## 后续优化建议

### 1. 页面管理
- 建立页面清单，确保所有页面都在pages.json中注册
- 定期检查页面跳转的有效性

### 2. 功能完善
- 实现缺失的联系人页面
- 实现忘记密码功能页面
- 完善其他缺失的功能页面

### 3. 错误监控
- 添加页面跳转错误监控
- 提供更好的错误提示和恢复机制

## 总结

通过本次修复，解决了以下问题：
- ✅ 水费页面跳转错误
- ✅ 城市选择页面无法访问
- ✅ 水费缴费页面无法访问
- ✅ 支付页面无法访问
- ✅ 不存在的页面跳转问题

现在生活缴费功能可以正常使用，页面跳转不再出现错误！🎉
