# 🚀 银行APP项目打包上线指南

## 📋 项目概述

**项目名称**: 银行APP  
**技术栈**: UniApp + Vue3 + 原生插件  
**支持平台**: H5、Android、iOS、微信小程序  
**核心功能**: 银行业务、防录屏保护、AI客服、财富管理

## 🛠️ 打包前准备

### 1. **环境检查**

```bash
# 检查Node.js版本
node --version  # 推荐 16+

# 检查npm版本
npm --version

# 检查项目依赖
npm list --depth=0
```

### 2. **代码优化检查**

```bash
# 运行代码检查
npm run lint:check

# 自动修复代码问题
npm run lint

# 格式化代码
npm run format
```

## 📱 各平台打包指南

### 🌐 **H5 Web端打包**

#### **生产环境打包**
```bash
# 清理缓存
npm run clean

# 打包H5
npm run build:h5
```

#### **部署文件位置**
```
dist/
├── build/
│   └── h5/
│       ├── index.html
│       ├── static/
│       └── assets/
```

#### **服务器部署**
```bash
# 将 dist/build/h5/ 目录内容上传到Web服务器
# 配置域名和HTTPS

# Nginx配置示例
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist/build/h5;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 防录屏功能需要HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name your-domain.com;
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    root /path/to/dist/build/h5;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 🤖 **Android端打包**

#### **开发版本打包**
```bash
# 清理缓存
npm run clean

# 打包Android
npm run build:app-android
```

#### **使用HBuilderX云打包**

1. **配置manifest.json**
   - 应用名称、版本号、图标
   - 权限配置
   - 原生插件配置

2. **云打包步骤**
   ```
   HBuilderX -> 发行 -> 原生App-云打包
   ├── 选择Android
   ├── 使用DCloud公用证书（测试）
   │   或上传自有证书（正式）
   ├── 选择打包模式：
   │   ├── 普通打包（标准版）
   │   └── 安全打包（加固版）
   └── 提交打包
   ```

3. **自签名证书生成**
   ```bash
   # 生成keystore文件
   keytool -genkey -v -keystore android-release.keystore \
           -alias android -keyalg RSA -keysize 2048 \
           -validity 10000
   ```

#### **离线打包**
```bash
# 1. 导出资源
npm run build:app-android

# 2. 配置Android Studio项目
# 3. 集成原生插件
# 4. 签名打包
```

### 🍎 **iOS端打包**

#### **开发版本打包**
```bash
# 清理缓存
npm run clean

# 打包iOS
npm run build:app-ios
```

#### **使用HBuilderX云打包**

1. **准备iOS证书**
   - Apple Developer账号
   - 开发证书/发布证书
   - 描述文件(Provisioning Profile)

2. **云打包步骤**
   ```
   HBuilderX -> 发行 -> 原生App-云打包
   ├── 选择iOS
   ├── 上传证书和描述文件
   ├── 配置Bundle ID
   └── 提交打包
   ```

3. **App Store发布**
   ```bash
   # 使用Xcode或Application Loader上传到App Store Connect
   # 提交审核
   ```

### 📱 **微信小程序打包**

#### **打包小程序**
```bash
# 清理缓存
npm run clean

# 打包微信小程序
npm run build:mp-weixin
```

#### **发布步骤**
```
dist/build/mp-weixin/ -> 微信开发者工具
├── 导入项目
├── 预览测试
├── 上传代码
└── 微信公众平台提交审核
```

## 🔧 生产环境配置

### 1. **环境变量配置**

创建 `.env.production` 文件：
```env
# API基础URL
VITE_API_BASE_URL=https://api.yourbank.com

# 微信小程序AppID
VITE_WECHAT_APPID=wx1234567890abcdef

# 其他配置
VITE_APP_TITLE=农业银行APP
VITE_APP_VERSION=1.0.0
```

### 2. **API接口配置**

更新 `src/utils/request.js`：
```javascript
const baseURL = process.env.NODE_ENV === 'production' 
    ? 'https://api.yourbank.com' 
    : 'http://localhost:3000'
```

### 3. **安全配置**

```javascript
// 生产环境关闭调试
if (process.env.NODE_ENV === 'production') {
    console.log = () => {}
    console.warn = () => {}
    console.error = () => {}
}
```

## 📋 版本管理

### 1. **更新版本号**

```javascript
// package.json
{
    "version": "1.0.0"
}

// manifest.json
{
    "versionName": "1.0.0",
    "versionCode": "100"
}
```

### 2. **Git标签管理**

```bash
# 创建版本标签
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0

# 查看所有标签
git tag
```

## 🔍 质量保证

### 1. **测试清单**

- [ ] **功能测试**
  - [ ] 登录注册流程
  - [ ] 转账支付功能
  - [ ] 生活缴费功能
  - [ ] 财富管理功能
  - [ ] AI客服对话

- [ ] **安全测试**
  - [ ] 防录屏功能验证
  - [ ] 指纹/Face ID认证
  - [ ] 数据加密传输
  - [ ] 会话管理

- [ ] **兼容性测试**
  - [ ] Android各版本
  - [ ] iOS各版本
  - [ ] 不同设备尺寸
  - [ ] 不同浏览器（H5）

### 2. **性能优化**

```bash
# 分析打包大小
npm run build:h5
# 检查 dist/build/h5/ 目录大小

# 图片压缩
# 代码分割
# 懒加载优化
```

## 🚀 部署流程

### 1. **准备部署清单**

```markdown
部署前检查清单：
- [ ] 代码已提交到主分支
- [ ] 版本号已更新
- [ ] 环境变量已配置
- [ ] 证书文件已准备
- [ ] 服务器环境已配置
- [ ] 数据库已迁移
- [ ] API接口已部署
```

### 2. **分阶段部署**

```bash
# 阶段1：测试环境部署
npm run clean
npm run build:h5
# 部署到测试服务器

# 阶段2：预生产环境部署
# 完整功能测试

# 阶段3：生产环境部署
# 正式发布
```

## 📱 应用商店发布

### **Android发布**

1. **Google Play Store**
   ```
   - 开发者账号注册
   - 应用信息完善
   - 截图和描述
   - 内容评级
   - 发布审核
   ```

2. **国内应用商店**
   ```
   - 华为应用市场
   - 小米应用商店
   - OPPO软件商店
   - vivo应用商店
   - 应用宝
   ```

### **iOS发布**

1. **App Store Connect**
   ```
   - 应用信息配置
   - 截图和预览视频
   - 版本信息
   - 价格和可用性
   - 提交审核
   ```

## 🔧 持续集成/持续部署 (CI/CD)

### GitHub Actions 配置示例

创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'
        
    - name: Install dependencies
      run: npm install
      
    - name: Build H5
      run: npm run build:h5
      
    - name: Deploy to server
      run: |
        # 部署脚本
        rsync -avz dist/build/h5/ user@server:/path/to/web/
```

## 📞 支持与维护

### 1. **监控配置**
- 应用性能监控
- 错误日志收集
- 用户行为分析

### 2. **更新策略**
- 热更新机制
- 版本兼容性
- 数据迁移方案

---

## 🎯 快速部署命令

```bash
# 完整部署流程
npm run clean
npm run lint
npm run build:h5          # Web端
npm run build:app-plus    # 移动端
npm run build:mp-weixin   # 小程序

# 验证部署结果
ls -la dist/build/
```

## 📋 部署后验证

1. **功能验证**
   - 访问 H5 版本URL
   - 安装APK/IPA测试
   - 小程序功能测试

2. **防录屏功能验证**
   - 访问 `/pages/test/mobile-protection-debug`
   - 测试各平台防录屏效果

3. **性能验证**
   - 页面加载速度
   - 接口响应时间
   - 内存使用情况

恭喜！🎉 您的银行APP项目已准备好上线部署！
