# 信用卡还款功能使用说明

## 功能概述

新增的信用卡还款功能提供了完整的还款流程，包括支付密码验证、数据更新和本地存储同步。用户可以通过此功能安全、便捷地完成信用卡还款操作。

## 功能特点

### 1. 安全验证
- **支付密码验证**：还款前必须输入正确的支付密码
- **余额检查**：自动检查账户余额是否足够
- **欠款验证**：确保还款金额不超过当前欠款
- **实时验证**：输入6位密码后自动提交验证

### 2. 数据更新
- **账户余额更新**：还款后自动扣除账户余额
- **信用卡信息更新**：更新信用卡当前余额和可用额度
- **交易记录**：自动记录还款交易到交易历史
- **本地存储同步**：所有数据同步到本地存储和user.json

### 3. 用户体验
- **直观界面**：清晰的还款信息展示
- **快捷操作**：提供快捷金额选择按钮
- **实时反馈**：操作结果实时反馈
- **错误处理**：完善的错误提示和处理

## 文件结构

### 1. API层 (`src/api/balance.js`)
新增的API函数：
- `verifyPaymentPassword(password)` - 验证支付密码
- `repayCreditCard(cardNumber, amount, paymentPassword)` - 执行还款操作
- `getCreditCards(cardNumber)` - 获取信用卡信息
- `getRepaymentRecords(cardNumber, limit)` - 获取还款记录

### 2. 组件层 (`src/components/common/RepaymentPasswordModal.vue`)
支付密码验证模态框：
- 还款信息展示
- 支付密码输入
- 密码验证逻辑
- 还款操作执行

### 3. 页面层 (`src/pages/credit-card/repayment.vue`)
还款页面：
- 信用卡选择
- 还款金额输入
- 快捷金额选择
- 还款确认
- 还款记录查看

## 使用流程

### 1. 访问还款页面
```javascript
uni.navigateTo({
  url: '/pages/credit-card/repayment'
})
```

### 2. 选择信用卡
- 页面自动加载用户的所有信用卡
- 显示每张卡的详细信息（额度、欠款、可用额度等）
- 点击选择要还款的信用卡

### 3. 输入还款金额
- 手动输入还款金额
- 或使用快捷金额按钮（100、500、1000、5000、全额还款）
- 系统自动验证金额是否合理

### 4. 确认还款信息
- 显示还款确认信息
- 包括信用卡信息、还款金额、还款后余额等
- 确认无误后点击"确认还款"

### 5. 输入支付密码
- 弹出支付密码验证模态框
- 输入6位支付密码
- 系统自动验证密码正确性

### 6. 完成还款
- 密码验证通过后执行还款操作
- 更新账户余额和信用卡信息
- 记录交易历史
- 显示还款成功提示

## 数据更新机制

### 1. 用户余额更新
```javascript
// 扣除还款金额
const newBalance = currentBalance - repaymentAmount
userInfo.balance = newBalance
```

### 2. 信用卡信息更新
```javascript
// 更新信用卡余额和可用额度
const newCardBalance = currentCardBalance - repaymentAmount
const newAvailableCredit = creditLimit - newCardBalance
```

### 3. 交易记录添加
```javascript
// 添加还款交易记录
addTransactionRecord({
  type: 'expense',
  amount: repaymentAmount,
  description: `信用卡还款 - ${cardType} ${cardNumber.slice(-4)}`,
  balance: newBalance,
  timestamp: new Date().toISOString(),
  icon: '💳',
  title: '信用卡还款'
})
```

### 4. 本地存储同步
```javascript
// 更新本地存储
uni.setStorageSync('userInfo', userInfo)
uni.setStorageSync('currentUser', userInfo)

// 更新本地数据库
updateUserBalanceInDatabase(userInfo)
```

## 安全机制

### 1. 密码验证
- 支付密码与用户数据中的`transactionPassword`字段比对
- 密码错误时显示错误提示，不执行还款操作
- 支持密码重试机制

### 2. 金额验证
- 检查还款金额是否为正数
- 验证还款金额不超过当前欠款
- 确认账户余额足够支付还款金额

### 3. 数据完整性
- 所有操作都有错误处理机制
- 数据更新失败时回滚操作
- 记录详细的操作日志

## 错误处理

### 1. 常见错误情况
- **支付密码错误**：提示"支付密码错误"
- **余额不足**：提示"账户余额不足，无法完成还款"
- **金额超限**：提示"还款金额不能超过当前欠款"
- **网络错误**：提示"还款失败，请重试"

### 2. 错误恢复
- 密码错误后可以重新输入
- 网络错误后可以重试操作
- 数据异常时自动回滚到之前状态

## 测试建议

### 1. 功能测试
- 测试正常还款流程
- 测试各种错误情况
- 验证数据更新正确性
- 检查交易记录完整性

### 2. 安全测试
- 测试密码验证机制
- 验证金额检查逻辑
- 测试异常数据处理

### 3. 用户体验测试
- 测试界面响应速度
- 验证操作流程顺畅性
- 检查错误提示清晰度

## 扩展功能

### 1. 可扩展的功能点
- 支持分期还款
- 添加还款提醒功能
- 支持自动还款设置
- 增加还款优惠活动

### 2. 技术优化
- 添加还款限额设置
- 支持多种支付方式
- 增加还款凭证生成
- 优化数据同步机制

## 注意事项

1. **数据安全**：确保支付密码等敏感信息的安全存储
2. **并发处理**：考虑多设备同时操作的并发问题
3. **数据备份**：重要操作前进行数据备份
4. **用户引导**：为新用户提供操作指导
5. **性能优化**：大量数据时考虑分页加载

---

*最后更新：2024年1月15日*
