# 快捷选择按钮布局调整说明

## 调整需求

用户要求调整信用卡还款页面的快捷选择按钮布局：
- **全额还款按钮居中显示**
- **其他金额按钮保持相同大小**

## 实现方案

### 1. HTML结构调整

#### 调整前
```html
<view class="quick-amounts-grid">
  <button v-for="amount in quickAmounts" :key="amount" class="quick-amount-btn">
    {{ amount === 'all' ? '全额还款' : `¥${amount}` }}
  </button>
</view>
```

#### 调整后
```html
<view class="quick-amounts-grid">
  <!-- 金额按钮行 -->
  <view class="quick-amounts-row">
    <button v-for="amount in [100, 500, 1000, 5000]" :key="amount" class="quick-amount-btn">
      ¥{{ amount }}
    </button>
  </view>
  <!-- 全额还款按钮 -->
  <view class="quick-amounts-full">
    <button class="quick-amount-btn full-repayment-btn">
      全额还款
    </button>
  </view>
</view>
```

### 2. CSS样式调整

#### 网格布局重构
```css
.quick-amounts-grid {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.quick-amounts-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
}

.quick-amounts-full {
  display: flex;
  justify-content: center;
  margin-top: 8rpx;
}
```

#### 全额还款按钮样式
```css
.full-repayment-btn {
  width: 200rpx;
  height: 80rpx;
  padding: 24rpx 20rpx;
}
```

## 布局效果

### 1. 金额按钮区域
- **布局**: 2x2网格布局
- **按钮**: ¥100, ¥500, ¥1000, ¥5000
- **大小**: 所有按钮大小一致

### 2. 全额还款按钮
- **位置**: 居中显示
- **大小**: 固定宽度200rpx，高度80rpx
- **样式**: 与其他按钮保持一致的视觉风格

## 技术实现

### 1. 布局策略
- 使用Flexbox + Grid混合布局
- 金额按钮使用Grid 2列布局
- 全额还款按钮使用Flexbox居中

### 2. 响应式设计
- 保持按钮间距一致
- 适配不同屏幕尺寸
- 保持视觉平衡

### 3. 交互保持
- 保持原有的点击效果
- 保持涟漪动画
- 保持选中状态样式

## 视觉效果

### 布局结构
```
┌─────────────────────────┐
│        快捷选择          │
├─────────┬───────────────┤
│  ¥100   │     ¥500      │
├─────────┼───────────────┤
│  ¥1000  │     ¥5000     │
├─────────┴───────────────┤
│      全额还款           │
└─────────────────────────┘
```

### 样式特点
- **统一性**: 所有按钮使用相同的样式系统
- **层次感**: 通过布局分组建立视觉层次
- **平衡性**: 全额还款按钮居中保持视觉平衡

## 代码优化

### 1. 数据分离
- 将金额数组从计算属性中分离
- 直接在模板中定义金额数组
- 提高代码可读性

### 2. 样式复用
- 保持基础按钮样式不变
- 通过额外类名添加特殊样式
- 避免样式重复

### 3. 维护性
- 清晰的HTML结构
- 模块化的CSS样式
- 易于后续调整

## 用户体验提升

### 1. 视觉层次
- 金额按钮和全额还款按钮分组显示
- 清晰的视觉分离
- 更好的信息组织

### 2. 操作便利
- 全额还款按钮居中，易于点击
- 金额按钮大小一致，操作体验统一
- 保持原有的快捷选择功能

### 3. 视觉平衡
- 整体布局更加平衡
- 符合用户视觉习惯
- 提升界面美观度

## 总结

通过这次布局调整，实现了：

✅ **全额还款按钮居中** - 使用Flexbox实现完美居中
✅ **金额按钮大小统一** - 使用Grid布局保证一致性
✅ **保持原有功能** - 所有交互和动画效果保持不变
✅ **提升视觉效果** - 更好的布局层次和视觉平衡

现在的快捷选择区域布局更加合理，用户体验得到进一步提升！🎉
